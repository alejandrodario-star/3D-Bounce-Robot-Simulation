# 3D Bounce Robot Simulation

Python simulation of a 3D underwater robot that moves in x, y, z and turns 90° clockwise on impact. Includes a real-time matplotlib 3D visualization.

## Run locally
```bash
pip install numpy matplotlib
python robot_simulation.py
```

## Launch in Binder
Click the badge below (after pushing this repo to GitHub; replace `USER` and `REPO`):

[![Binder](https://mybinder.org/badge_logo.svg)](https://mybinder.org/v2/gh/USER/REPO/HEAD?labpath=simulation.ipynb)

Binder uses the included `environment.yml` to set up Python + libraries, then opens `simulation.ipynb`.
